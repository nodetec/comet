import { Compartment } from "@codemirror/state";

const fontSize = new Compartment();

const fontFamily = new Compartment();

const fontWeight = new Compartment();

const lineHeight = new Compartment();

export { fontSize, fontFamily, fontWeight, lineHeight };

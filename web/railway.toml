[build]
buildCommand = "pnpm --filter @comet/web build"
watchPatterns = ["web/**", "packages/**", "pnpm-lock.yaml"]

[deploy]
startCommand = "pnpm --filter @comet/web start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
